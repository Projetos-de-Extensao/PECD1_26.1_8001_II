# Perfil Horas AAC — React (Vite)

Projeto mínimo criado para demonstrar a conversão do protótipo "Comprovante AAC" para React + Vite.

Instalação e execução:

```bash
cd react-app
npm install
npm run dev
```

O componente principal está em `src/components/Comprovante.jsx`.
